@using BlazorClient.Logic.Interfaces.Communication.Hub

    <nav class="navbar navbar-expand-lg navbar-dark" >
        <a class="navbar-brand" href="#">
            <img src="/images/fox_black.png" width="40" class="d-inline-block align-top" style="filter: invert(100%)" alt="">
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <NavLink class="nav-link h5" href="#">Home </NavLink>
                </li>
                <AuthorizeView>
                    <Authorized>
                        <li class="nav-item h5">
                            <NavLink class="nav-link" href="/the-showcase">The Showcase</NavLink>
                        </li>
                        @* <li class="nav-item h5"> *@
                        @*     <NavLink class="nav-link" href="/about-me">About Me</NavLink> *@
                        @* </li> *@
                    </Authorized>
                </AuthorizeView>
            </ul>
            <div class="d-flex flex-row align-items-center">
                <CartBadge/>
                <LoginDisplay/>
            </div>
        </div>
    </nav>

@code
{
}